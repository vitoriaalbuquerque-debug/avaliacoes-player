fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic-settings==2.5.2
python-multipart==0.0.9
httpx==0.27.2
python-dotenv==1.0.1
pytest==8.3.3

# Só necessário se DATABASE_URL apontar pra Postgres (ex: Neon/Supabase no
# piloto). SQLite (o padrão local) não precisa disso — é por isso que
# tiramos daqui: esse pacote precisa compilar C na instalação e foi a causa
# de vários erros no Windows durante o setup local. Instale à parte quando
# for migrar pro piloto: pip install psycopg2-binary
